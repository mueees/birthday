function Response(data, stream){
    this._idStream = stream._id;
    this.stream = stream;
    this.write = this.send;
}

Response.prototype = {
    send: function(message){
        this.stream.write( JSON.stringify(message) );
    }
}

module.exports = Response;

