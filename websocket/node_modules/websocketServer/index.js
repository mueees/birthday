var WebSocketServer = require('./server');
var crossroads = require('crossroads');
var route = require('routes/websocket');

//add routing
route(crossroads);

module.exports = function(server){
    new WebSocketServer(server, crossroads);
}