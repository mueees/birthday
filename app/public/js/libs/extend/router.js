define([
    'marionette'
], function(Marionette){

    /*var protoSlice = Array.prototype.slice;
    function slice(args) {
        return protoSlice.call(args);
    }

    return Marionette.AppRouter.extend({
        before: function(options){
            console.log(1);
            Marionette.AppRouter.prototype.before(this, slice(arguments));
        }

    })*/

})