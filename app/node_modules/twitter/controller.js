var controller = {
    addLilsteners: function( res, req, next  ){
        next( new Error("So many listeners. Please try later...") );
    },
    removeLilstener: function( res, req, next  ){
        req.send("all right");
    }
}

module.exports = controller;