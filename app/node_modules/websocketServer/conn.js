function Conn(conn){
    this.conn = conn;
    this.write = this.send;
}

Conn.prototype = {
    send :function(message){
        this.conn.write( JSON.stringify(message) );
    }
}

module.exports = Conn;
